# Spring Boot Server Port
server.port=8080

# Database Settings (Plain JDBC via DatabaseConnection utility)
db.url=jdbc:mysql://localhost:3306/petsafe_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=root

# Networking TCP Socket Server Port
socket.server.port=9090

# JWT Security Settings
jwt.secret=PetSafeSuperSecretKeyForJWTTokenGeneration2026AcademicProject
jwt.expiration=86400000

# Logging
logging.level.com.petsafe=INFO
